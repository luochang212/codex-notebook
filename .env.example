# Local Git identity used by agents when creating or amending commits.
# Copy this file to `.env` and fill in local values. Do not commit `.env`.
GIT_AUTHOR_NAME=""
GIT_AUTHOR_EMAIL=""
GIT_COMMITTER_NAME=""
GIT_COMMITTER_EMAIL=""
